# 安全政策

easyssh 托管 ACME 私钥、证书与 SSH 凭据,**任何安全问题都可能造成凭据泄露**。
请务必通过私密渠道报告,不要在公开 issue 中提交。

## 报告漏洞

**不要公开提交 issue。** 请通过以下方式私下联系维护者:

- 邮件:`z1973135690@gmail.com`
- 或 GitHub Security Advisory(仓库 → Security → Report a vulnerability)

请在报告中包含:

- 影响的版本(commit hash 或 tag)
- 漏洞类型(如路径穿越、私钥泄露、中间人、越权访问等)
- 复现步骤或 PoC(注意脱敏,不要包含真实私钥/配置)
- 影响范围与你的修复建议(如有)

## 处理承诺

- 收到报告后 **48 小时内**确认收悉
- 确认有效后尽快修复,并在修复发布前不公开漏洞细节
- 修复发布时在 CHANGELOG 中记录,并致谢报告者(经同意后)

## 安全相关配置建议

- SSH 部署**必须**配置 `known_hosts`,未配置时 easyssh 会拒绝连接(防中间人)
- 密钥一律用 `{{env:VAR}}` 引用环境变量,不要明文写入配置文件
- 含私钥/密钥的 `.zsbundle` 导出包经加密通道传递,口令即唯一防线
- 私钥文件权限保持 0600
